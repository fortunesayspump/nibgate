{
  "name": "nibgate-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for the Nibgate app, CLI package, and example origin sites.",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build:frontend && npm run dev:backend",
    "dev:demo": "npm --prefix demo install && npm --prefix demo run dev",
    "dev:cli": "npm --workspace nibgate run dev",
    "dev:backend": "npm --workspace @nibgate/backend run dev",
    "dev:frontend": "npm --workspace @nibgate/frontend run dev",
    "build:frontend": "npm --workspace @nibgate/frontend run build",
    "routes": "npm --workspace nibgate run routes"
  }
}
